<!-- Esta vista por los momentos no hace nada (no se elimino por seguir convenciones) -->



<%= form_for @ask do |f| %>
  <% if @ask.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@ask.errors.count, "error") %> prohibited
      this article from being saved:</h2>
    <ul>
    <% @ask.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :campo %><br>
    <%= f.text_area :campo %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
