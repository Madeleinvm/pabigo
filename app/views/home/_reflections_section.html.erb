<div class="slider">
  <div id="about-slider">
    
    <div class="text-center">
      <h3>Reflexiones</h3>
    </div>

    <div id="carousel-slider" class="carousel slide" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators visible-xs">
          <% for i in 1..@total_reflections %>
            <li data-target="#carousel-slider" data-slide-to="<%= i %>"></li>
          <% end if @total_reflections > 0 %>
        </ol>

        <div class="carousel-inner">
          <% @reflections.each do |reflection| %>
            <div class="item">
              <%= image_tag(reflection.image.picture.url, class: "img-responsive") %>
              <!-- <img src="img/7.jpg" class="img-responsive" alt=""> -->
              <div class="carousel-caption">
                <div class="wow fadeInUp" data-wow-offset="0" data-wow-delay="0.3s">
                  <h2><span>Eventos & Jornadas Especiales</span></h2>
                </div>
                <div class="wow fadeInUp" data-wow-offset="0" data-wow-delay="0.6s">
                  <p>Esterilización, Adopción, Vacunación</p>
                </div>
              </div>
            </div>
          <% end unless @reflections.empty? %>
        </div>

      <a class="left carousel-control hidden-xs" href="#carousel-slider" data-slide="prev">
        <i class="fa fa-angle-left"></i>
      </a>

      <a class=" right carousel-control hidden-xs" href="#carousel-slider" data-slide="next">
        <i class="fa fa-angle-right"></i>
      </a>
    </div>
    <!--/#carousel-slider-->
  </div>
  <!--/#about-slider-->
</div>
<!--/#slider-->
